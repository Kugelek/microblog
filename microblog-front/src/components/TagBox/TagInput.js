import { withStyles } from '@material-ui/core/styles';
import { Input } from '@material-ui/core';
export const TagInput = withStyles({
    root: {
        width: '10vw',
        marginBottom: '5rem',
       
    },
    
  })(Input);